server:
  port: 8081

spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: 'jdbc:postgresql://db:5432/creditCalculator'
    username: postgres
    password: postgres

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: none